/**
 *      Formula  of Array
 *      Array means - multiple value store
 *      example  : 
 *          let numbers = [1, 3, 7]; [] = squre braket
 *          console.log(numbers)
 * 
 *      array show the index serially as like, 0 : 1
 *                                             1 : 3
 *                                             2 : 7
 *                                             length : 3
 *                                             -Proto- Array(0)
 *      NOTE : 1. Any kind of datastore as like (String, Number) use in a single array
 *             2. But we should concern about try ti write same type in single array.
 * 
 *  # Object store in array:
 *      Example:
 *          let cars = [
 *          {brand:"Audi", price= 45000}
 *      ]  
 *          console.log(cars);
 * 
 *  # How to access specific value 
 *      FORMULA:
 *          let numbers = [1, 3, 7];
 *              console.log(numbers[index number]);
 *              example: console.log(numbers[1]);
 * 
 *  # How to change array value
 *      EXAMPLE :
 *          let number = [1, 3, 7];
 *          console.log (numbers [index number]);
 *          console.log (numbers [1]);
 * 
 *  # How to chage value in ARRAY
 *      FORMULA:
 *          numbers[0] = 4;  // New values ​​have been assigned to the index.
 *          console.log (number[0]);
 * 
 *  # How to change value in OBJECT:
 *      FROMULA:
 *          let cars = [
 *              {brand: "Audi", Price: 45000}
 *      ];
 * 
 *      cars [1].brand = "Honda" // Specific Change Object
 *      console.log (cars[1].brand); // Specific Call Object
 *  
 * */