// there are 2 types of language

// Static (Statically-typed) ==== { it cannot be change in future. }
costumerName = "John";

// Dynamic (Dynamic-typed) ==== { JavaScript the type of a variable can change at runtime. }
let costumerName = "John";

//test with typeof method in console.